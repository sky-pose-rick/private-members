extends layout 

block content 
  h1 Become #{rank}

  form(method='POST' action='')
    label Password: 
      input(type='password' required='true' name='password')
    button(type='submit') Submit
  
  if errors 
    ul 
      for error in errors 
        li=error.msg